# PP-OCRv6 Studio — webapp dependencies
# Install: pip install -r requirements-webapp.txt
# Tested on macOS (Apple Silicon) with Python 3.10+

fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
Pillow>=10.0.0
numpy<2.0.0
onnxruntime>=1.17.0          # use onnxruntime-silicon on Apple M-series for CoreML
PyYAML>=6.0
openpyxl>=3.1.0

# Optional: faster inference on Apple Silicon
# pip install onnxruntime-silicon  # replaces onnxruntime above
